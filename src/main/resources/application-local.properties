# Local development profile
# - Recreate schema and seed test data from data.sql on each startup
spring.jpa.hibernate.ddl-auto=create
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
